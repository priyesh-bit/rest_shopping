import 'package:flutter/material.dart';
import 'package:socket_io_client/socket_io_client.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Demo',
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(
          seedColor: Colors.deepPurple,
        ),
        useMaterial3: true,
      ),
      home: const Client(),
    );
  }
}

class Client extends StatefulWidget {
  const Client({super.key});

  @override
  State<Client> createState() => _ClientState();
}

class _ClientState extends State<Client> {
  late Socket socket;

  @override
  void initState() {
    socket = io(
      'https://4ed9-2401-4900-1c02-123b-fd7b-b8bb-1a50-1a32.ngrok-free.app/chat',
      <String, dynamic>{
        'transports': ['websocket'],
        'extraHeaders': {
          'token':
              "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjoiNjUwM2YxNjY2ZTc1MDE3ZjEwOTYyM2EyIiwiZW1haWwiOiJ0ZXN0QGxvZ2ljLmNvbSIsImlhdCI6MTY5NTEyNjAyNywiZXhwIjoxNjk1MjEyNDI3fQ.opgyLLqea6WdpZam1B6-FxYeVGqiSsLJR0QTPioIYYM"
        },
      },
    );

    socket.onConnect((data) {
      print('connect');
    });

    // Listen for the authenticated event
    socket.on('authenticated', (data) {
      print(data);
    });
    // Listen for the authenticated event
    socket.on('user-6503f1666e75017f109623a2', (data) {
      print(data);
    });

    socket.onDisconnect((_) => print('disconnect'));
    super.initState();
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Client'),
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            TextField(
              decoration: const InputDecoration(
                hintText: 'Enter a message...',
              ),
              onSubmitted: (message) {
                // Send the message to the server
                socket.emit('user-6503f1666e75017f109623a2', message);
              },
            ),
          ],
        ),
      ),
    );
  }
}
